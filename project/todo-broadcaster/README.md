# Todo Broadcaster

simple subscriber to NATS that sends messages to discord via webhook

To build and upload image to docker.io registry run the following

```sh
docker build -t lucksei/todo-broadcaster . && docker push lucksei/todo-broadcaster:latest
```
